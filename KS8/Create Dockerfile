Craete a Dockerfile for my webpage for Syunsi
# Create a Dockerfile
# keep index.html in the root folder e.g. C:\Users\DELL> in case of W10 Docker Desktop

FROM ubuntu:latest
ENV DEBIAN_FRONTEND=noninteractive 
RUN apt-get update && apt-get install apache2 -y && apt-get install apache2-utils -y && apt-get install curl && apt-get clean 
EXPOSE 80
Copy index.html var/www/html/ 
HEALTHCHECK CMD curl --fail http://localhost:80/ || exit 1
ENTRYPOINT service apache2 restart && bash
